# Label-register publications (ADR-014): generated but tracked, owned
# by generate-label-registers; the README is authored planning text.
plans_labels_files = files('README.md')

specification_register_file = files('specification.md')
realization_register_file = files('realization.md')

# The companion attestation register (ADR-020): generated by the
# acceptee's own generator from the archived registry, its adopting
# record, and a corpus-wide mint census.
attestation_register_file = files('attestation.md')
