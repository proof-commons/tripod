# `cli-common`

`cli-common` is the shared command-line scaffolding this workspace's tools are
built on: argument parsing, the JSON output contract, the control-plane record
kinds, and the exit classes every helper binary uses.

## Acknowledgements

We thank the Torrust developers for the inspiration for this command-line tooling.
